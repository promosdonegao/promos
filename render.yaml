services:
  - type: web
    name: promos-tracking
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn tracking_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
